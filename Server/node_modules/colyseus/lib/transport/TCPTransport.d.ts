/// <reference types="node" />
import * as net from "net";
import { Transport } from './Transport';
import { MatchMaker } from './../MatchMaker';
import { ServerOptions } from './../Server';
export declare class TCPTransport extends Transport {
    constructor(matchMaker: MatchMaker, options?: ServerOptions);
    listen(port?: number, hostname?: string, backlog?: number, listeningListener?: Function): this;
    protected onConnection: (client: any) => void;
    shutdown(): void;
    protected onMessage(client: net.Socket & any, message: any): void;
}
